# Smartcache
High-performance Python caching library featuring TTL, LRU, async support, and disk/memory backends. Uniquely handles unhashable arguments (dicts, lists) out of the box. Boost speed and cut API costs with a simple @cache decorator.
